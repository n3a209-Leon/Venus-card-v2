# Venus Card System v2.8.10

## 幹部區塊不再顯示組長／秘書長

- 幹部按鈕的小字只顯示 LIMU 的實際職稱。
- 組長與秘書長已是獨立區塊，幹部區塊不再重複標示。
- 沒有幹部職稱的座號不顯示任何小字。

## 修正幹部名單帶不進來（非同步競態）

- 分組（`groupVersions:`）與幹部名單（`officers:`）是兩個獨立的非同步讀取，
  先前共用一個 `roleSeeded` 旗標，先回來的那份會把旗標鎖上，
  後到的那份永遠不會帶入。
- 實際症狀：組長／秘書長各 5 人正常帶入，幹部名單卻始終顯示「尚未設定」，
  即使來源已顯示「LIMU 11 人」。
- 改為 `_fixed.seeded` 逐份記錄，三份名單各自獨立判斷。
- 來源尚未載入時不標記，下次 render 再試。
- 已有名單時只標記、不覆蓋手動調整。
- 舊的 `roleSeeded` 旗標會升級為組長／秘書長已帶入，幹部維持未帶入以便補上。
- 手動「↻ 重新帶入」也會一併更新該份的標記。

## 保護範圍

- Venus OS 與 LIMU 資料維持唯讀：`officers:*`、`groupVersions:*`、`classes`
  均只使用 `.get()`，無任何寫入。
- 總覽個別座號資訊、欄位與計算邏輯未修改。
- 發卡紀錄仍為 `k:'幹部'`，`cardCounts()` 與總覽欄位不受影響。
