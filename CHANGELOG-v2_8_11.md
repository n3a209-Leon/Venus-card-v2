# Venus Card System v2.8.11

純樣式調整，JavaScript 除版本號外未修改。

## 底部導覽改為懸浮膠囊（IG 風格）

- 導覽列改為離開螢幕邊緣的圓角膠囊，左右內縮 14px（窄螢幕 8px），
  底部離螢幕 8–28px（依安全區）。
- 作用中分頁改用膠囊底色，取代原本頂端的細線指示。
- 按下時改為輕微縮放回饋，取代原本的背景變色與位移。
- 五鍵配置、圖示、文字與 `goTab()` 行為完全未變。

## 觸控與讓位

- 按鍵高度維持 54px，各機型按鍵寬度 68–83px，仍高於 Apple HIG 建議的 44×44。
- 命中區不再向下延伸至螢幕底部。膠囊下方已是內容區，
  舊版的延伸會攔截該處的點擊。
- `--tabar-h` 改為「按鍵高 + 離底距離」，`scroll-area`、`toast`、`undo-fab`
  自動沿用，內容與浮動元素皆不會被膠囊遮住。

## 效能

- 移除導覽列的 `backdrop-filter`。固定元素上的毛玻璃是 iPhone 捲動卡頓的
  主因之一；改用高不透明度背景（0.97–0.99），視覺差異極小。

## 四套主題

- 慕夏、NERV、星雲、預設皆已改為膠囊外框與對應的作用中底色。
- 移除三處僅適用於全寬導覽列的頂端漸層細線。
- 修正稍後覆寫的樣式規則：星雲主題原本會把作用中膠囊塗成實心紫色
  （那是舊版頂線的顏色）。

## 保護範圍

- Venus OS 與 LIMU 資料維持唯讀。
- 幹部／組長／秘書長三區塊、總覽、成績、報表、同步邏輯均未修改。
