# Venus Card System v2.8.8

## 幹部名單改讀 LIMU 教師工具（唯讀）

- 路徑：`users/{uid}/data/officers:{classId}`，讀取 `value` 欄位。
- `value` 格式為 `{ "1":[{role,seat},…], "2":[…] }`，`"1"` 上學期、`"2"` 下學期。
- **嚴格唯讀**：只使用 `.get()`，不對 `officers:*` 或 `classes` 執行任何
  `set` / `update` / `delete`，以免打亂 LIMU 的 `revision` 版本比對。
- 需以同一個 Google 帳號登入，Firestore 規則才允許讀取。
- 使用 Firebase Web SDK v9 compat（沿用專案既有載入方式，未新增相依）。

## 職稱顯示

- 幹部按鈕小字改顯示實際職稱，例如「班長」「學藝股長」。
- 一人兼多職時顯示第一個職稱並標示 `+N`，完整清單在長按提示（title）中。
- 職稱為使用者自訂字串，一律經 `esc()` 跳脫後才進 HTML。
- 沒有職稱資料時，沿用原本的 Venus OS 組長／秘書長標示。

## 學期切換

- 幹部區塊新增上／下學期切換鈕。
- 首次載入依 `settings:main` 的 `semStart1` / `semEnd1` / `semStart2`
  自動判斷目前學期，抓不到則以月份粗估。
- 右側顯示名單來源與筆數，例如「LIMU 12 人｜2026-07-31」。

## 自動帶入

- 幹部名單自 LIMU 自動帶入，每人 ×1。
- 一人兼多職不會加倍給卡，卡片以「人」為單位計算。
- 不在座號名單內的座號自動略過。
- 只在該班首次載入時帶入一次，之後尊重手動調整；另有「↻ 重新帶入」按鈕，
  覆蓋前會確認且可撤銷。

## 容錯

- 文件不存在、`value` 為空、學期不存在或為空陣列時，顯示「尚未設定幹部」而非報錯。
- Venus Card 與 LIMU 的 classId 若不同源，會改讀 `users/{uid}/data/classes`
  以班名對照後再試一次。

## 診斷

- 🔎 Venus OS 資料掃描新增幹部名單區段：文件 ID、`updatedAt`、`revision`、
  兩個學期各自的筆數與座號職稱對照。

## 保護範圍

- Venus OS 與 LIMU 資料維持唯讀，未新增任何寫入、更新或刪除。
- 總覽個別座號資訊、欄位與計算邏輯未修改。
- 發卡紀錄仍為 `k:'幹部'`，`cardCounts()` 與總覽欄位不受影響。
