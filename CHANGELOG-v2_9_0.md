# Venus Card System v2.9.0 — 可靠度升級（第一批）

## 同步狀態文字化

- 標題列的 7px 圓點改為可點的狀態晶片，直接顯示文字：
  `14:32`（已同步）／`待同步`／`離線`／`未登入`／`失敗`。
- 非「已同步」狀態一律在標題列下方顯示橫幅說明原因，
  同步失敗為紅色並可點擊重試。
- 點狀態晶片可看完整明細：本機最後成功同步時間、資料最後變更時間、
  revision、是否有未上傳變更、登入與網路狀態、同步模式、最近錯誤訊息。

### 修正一個會誤報「已同步」的問題

`_lastSyncedAt` 在 `mergeSnapshots()` 走 `Math.max(local, remote)`，
是跨裝置合併後的最大值，不代表這台裝置成功上傳過。
沿用它會在「另一台手機同步過、這台其實從未成功」時顯示綠燈。

新增只存在本機、不進 snapshot 的 `_lastSyncOkAt`，逐班記錄，
僅在本機成功上傳、或載入時確認雲端已含本機資料時更新。

另外，剛登入時不再直接顯示「已同步」——此時尚未上傳，改由實際結果決定。

## 減少動態效果

- 新增 `prefers-reduced-motion` 支援，配合 iPhone 設定 →
  輔助使用 → 動作 → 減少動態效果。
- 動畫改為瞬間完成並停在最終狀態，同時中止無限循環；
  不使用 `animation:none`，因為部分元素靠動畫結束後才進入最終外觀。
- NERV 主題的持續閃爍、開場畫面的光影循環、同步中的脈動一律關閉。

## 觸控目標

- `.btn-xs`（多用於「刪」等破壞性操作）最小高度由 28px 提高到 36px。
- `.btn-xs` 與兌換卡的「本次不換」另加透明命中區，
  總命中區至少 44×44，符合 Apple HIG 建議。
- 總覽座號卡未修改。

## 死碼清除

- 移除五個定義後從未使用的 `@keyframes`：`scanline`、`purple-glow-pulse`、
  `nebula-drift`、`star-twinkle`，以及僅供離線使用的 `#offline-banner` 樣式。

## 保護範圍

- Venus OS 與 LIMU 資料維持唯讀。
- 總覽個別座號資訊、欄位與計算邏輯未修改。
- 合併邏輯（LWW + tombstone）未修改。

## 未包含（下一版）

- `classCloudDocId()` 仍為 `{cls.year}__{classId}`。建議改為
  `where('classId','==',classId)` + `mergeVisibleSnapshots()`，
  使孤兒文件自動併回。此項動到同步層，需先執行 🛟 資料診斷與救援確認現況。
- 學年度轉換、持久化回收筒、Google Sheets 隱私保護。
