# Venus Card System v2.8.5

診斷工具修正版，不含功能或資料結構變更。

## Venus OS 資料掃描（修正與加深）

- 新增掃描 `settings:main`。這是 Venus OS 的全域設定文件，`_groups` 實際上
  是從這裡的 `groups` 欄位讀取，不是 `hw5ren:groups:`。v2.8.4 的掃描只查
  classId 前綴與 `hw5ren:` 前綴，兩者都不符合 `settings:main`，因此整份漏掃。
- 輸出往下再挖一層：陣列列出第一筆物件的欄位，物件列出子鍵，
  純值列出型別與內容摘要。
- `classes`、`groups`、`students`、`roster` 四個欄位額外展開第一筆內容，
  幹部職稱最可能存放於此。
- 每日文件除份數與欄位聯集外，另附一份範例文件的完整結構。
- `_groups[0]` 的完整欄位一併列出。
- 掃描結果自動複製到剪貼簿，不必再以截圖傳遞。

## 保護範圍

- 純唯讀診斷，未新增任何寫入、更新或刪除。
- 幹部／組長／秘書長三區塊、抵免載入、同步、總覽、報表均未修改。
