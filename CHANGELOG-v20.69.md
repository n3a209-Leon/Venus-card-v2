# LIMU v20.69 — 新增日蝕主題

版本：20.69.0
建置：limu-teacher-v20-69-20260804
快取：hw-tracker-v20-69
資料格式：7（未變動）

主題由五套增為六套。日蝕是**新增**的第六套，原本的「奧德賽・遠征」
（青銅、海圖、羊皮紙淺色）原封不動保留。

---

## 色票來源

參照獎卡系統 v3.3.4 的 `ECLIPSE ODYSSEY` 區塊：

| 角色 | 色值 |
|---|---|
| 底色 | `#12151C` |
| 玫瑰灰 | `#C2A2B7` |
| 金 | `#D2AD76` |
| 文字 | `#F2EBEF` |

## 做法

LIMU 的底色是淺色，所以深色主題需要覆寫的選擇器很多——星雲主題有 150 條。
日蝕要覆寫的**是同一組選擇器**（同樣的版面骨架、同樣的深色需求），差別只在色票。

因此沒有逐條手挑，而是把星雲的規則做 HSL 分區換算：

| 原色域 | 轉換 |
|---|---|
| 藍紫 195–300° | → 玫瑰灰 322°，彩度上限 .26 |
| 深底 L < 22% | → 炭灰 222°，重新落到 7.5–20% |
| 金 25–60° | → 對齊 33° |
| 綠／紅 | 保留色相（狀態色有語意），僅稍降彩度 |
| 純黑／純白 | 不動（陰影與高光） |

深色那一段特別處理過：星雲最深到 `#04050f`（L≈4%），等比縮放會逼近純黑，
而日蝕的底是 `#12151C`（L≈9%）。把 0–22% 重新落到 7.5–20%，最深的底色
仍看得出是炭灰而不是黑洞。

換算後對照獎卡系統的錨點：

| 目標 | 換算結果 |
|---|---|
| `#12151C` | `#15171C` |
| `#C2A2B7` | `#BE91AE` |
| `#D2AD76` | `#D7B081` |
| `#F2EBEF` | `#ECE3E9` |

共轉出 **143 條規則**。

## 裝飾層另外手寫

星圖專屬的東西一律不沿用——否則日蝕只會是「換色的星雲」。跳過的有：

- 星點字元裝飾（`· ✦ · ✧ · ⋆`）
- `nebula-route-v20-62.svg` 航線
- `nebula-frame-v20-62.svg` 星盤外框
- `nebula-divider-v20-62.svg` 分隔線

日蝕改用自己的日冕構圖：上緣一圈金光、正中央壓一塊暗做出「蝕」的缺口、
玫瑰灰餘暉往兩側散開、底部壓暗讓內容區浮起來，加一道細金線橫過頁首上緣。

**全部用漸層完成，不引入任何圖檔**——離線清單維持 28 項，Service Worker
的預快取內容完全沒動。

主題選單的圖示也是手繪 SVG（金色日冕在外、暗面偏左上、缺口留右下），
沒有用 `🌘` emoji，因為各家字體的相位與留白都不一樣。

## 接進去的地方

| 位置 | 內容 |
|---|---|
| `THEMES` | 新增 `{ id:'eclipse', name:'日蝕', desc:'炭灰、日冕金與玫瑰灰餘暉' }` |
| `isEclipse` | 新旗標 |
| `themeColors` | `eclipse: #12151c`（iOS 狀態列） |
| `--card-bg` 色票組 | 第六組，直接用獎卡系統的 token |
| 頁首主題鈕、功能面板、主題選單面板／標題／選中底色／分隔線／勾選 | 各補一條 eclipse 分支 |
| `renderThemeGlyph` | 新增 eclipse 圖示 |
| Venus OS Toast | 由換算規則自動涵蓋，五套變六套 |

---

## 風險說明

這是個大 diff（168 個 CSS 選擇器 + 7 處 JS 分支），但**風險是隔離的**：
所有新規則都掛在 `[data-theme="eclipse"]` 之下，不選這個主題就完全不會生效。
既有五套主題的 CSS 一個字都沒改。

## 驗證紀錄

- 11 段內嵌程式 + 合併後全域範圍語法：通過
- ESLint 錯誤規則：30 項，與 v20.64 起完全相同，五個版本下來未新增任何一項
- `react-hooks/rules-of-hooks`：零違反
- 逐項斷言 16 項全過（含 v20.65～v20.68 迴歸）
- CSS 大括號與註解配對：平衡
- 日蝕規則中無 `url()`、無星圖符號：確認
- 預快取 28 項未變動，無缺檔
- 版本識別一致性：通過

**未做真實瀏覽器驗證**——Chromium 在本次作業環境無法下載。

### 建議的實測順序

1. 更多 → 主題 → 日蝕。先看整體對比是否足夠（換算是機械的，個別畫面可能偏暗或偏亮）。
2. 重點看這幾處：月曆格子、當日紀錄表格、輔導紀錄、設定頁、功能面板。
3. 頁首上緣的細金線與日冕位置是否協調。
4. 跳一則 Toast（例如按「同步」），確認配色跟著日蝕走。
5. 切回其他五套主題各看一眼，確認沒有被影響。

換算出來的色票若有哪一處偏掉，直接告訴我是哪個畫面、偏暗還是偏亮，
我改對映區間重新產一次即可——不需要逐條手改。
