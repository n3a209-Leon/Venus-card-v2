# Venus Card System v2.8.6

診斷工具強化版，不含功能或資料結構變更。

## 診斷輸出改用可捲動視窗

- 🩺 抵免診斷、🔎 Venus OS 資料掃描、🛟 資料診斷的報告改用專用視窗顯示，
  可捲動、可全選，並附「📋 複製」按鈕。
- `alert()` 在長文字會被截斷且無法複製，先前只能靠截圖傳遞。
- 資料救援流程中需要使用者決定的 `confirm()` 維持原樣，因為它必須阻擋執行。

## 掃描內容加深

- 改為遞迴展開，不再只白名單展開 `classes` / `groups` / `students` / `roster`。
  上一版因此看不到 `officerPresets` 等欄位的內容。
- 任何物件陣列都會列出前 3 筆的完整內容，其餘標示為同結構。
- 純值陣列會列出前 8 個實際值（例如 `members：陣列 5 筆：1, 2, 3, 4, 5`）。
- 字串值顯示長度由 40 字提高到 60 字，欄位上限由 20 提高到 40。
- `settings:main` 與每日文件範例展開至第 3 層，分組展開至第 2 層。

## 保護範圍

- 純唯讀診斷，未新增任何寫入、更新或刪除。
- 幹部／組長／秘書長三區塊、抵免載入、同步、總覽、報表均未修改。
