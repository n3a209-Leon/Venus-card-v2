# Venus Card System v2.8.1

## 慕夏主題

- 移除主框、卡片與彈窗的 `border-image` 拉伸。
- 862×1825 主框依原始比例置頂，長頁面以細金線自然延續。
- 卡片與彈窗改用左右獨立花飾；每張素材只出現一次並保持比例。
- 保留鳶尾、空狀態與底部花飾的原始比例，不新增重複圖樣。

## 星雲主題

- 新增非重複的漸層星軌、角落星芒與內側細框。
- 頁面、卡片、統計卡、彈窗、頁首及底部導覽採不同層級的點綴。
- 所有裝飾皆不接收觸控，不增加 padding，也不改按鍵與操作區尺寸。

## 不回退邊界

- 總覽個別座號資訊、欄位、計算與點擊操作完全不變。
- 班級新增、改名、封存、恢復及 OS 班級隱藏邏輯不變。
- Venus OS 來源維持只讀，不新增寫入、更新或刪除。
- Firebase、同步、救援、Excel、報表與底部導覽尺寸不變。
